semantic_models:
  - name: expense_reserve
    model: ref('expense_reserve')
    description: "Table containting expense_reserve information."
    entities:
      - name: claim_amount
        type: primary
        expr: Claim_Amount_Identifier
    dimensions:
      - name: has_expense_reserve
        type: categorical
        expr: "1"