semantic_models:
  - name: premium
    model: ref('premium')
    description: "Table containting premium information."
    entities:
      - name: policy_amount
        type: primary
        expr: Policy_Amount_Identifier
    dimensions:
      - name: has_premium
        type: categorical
        expr: "1"
